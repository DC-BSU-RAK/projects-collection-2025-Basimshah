<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.wescents">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WeScents"
        tools:targetApi="31">

        <!-- Splash screen activity as launcher -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Home screen -->
        <activity android:name=".MainActivity" />

        <!-- Perfume Detail Pages -->
        <activity android:name=".DiorActivity" />
        <activity android:name=".CKActivity" />
        <activity android:name=".JeanPaulActivity" />
        <activity android:name=".ShalimarActivity" />

        <!-- Cart Page -->
        <activity android:name=".CartActivity" />
    </application>
</manifest>
